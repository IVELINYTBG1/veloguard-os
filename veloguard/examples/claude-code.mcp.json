{
  "_comment": "Attach Claude Code (or any MCP client) to VeloGuardOS's kernel tools, through the guard. Copy to your project's .mcp.json. Autonomy: read | guarded | full. Set VELOGUARD_APPLY=1 to enforce for real (default dry-run).",
  "mcpServers": {
    "veloguard": {
      "command": "veloguard-mcp",
      "env": {
        "VELOGUARD_AGENT_AUTONOMY": "guarded",
        "VELOGUARD_APPLY": "0"
      }
    }
  }
}
