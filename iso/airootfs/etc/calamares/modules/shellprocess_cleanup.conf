---
# Strip the live-ISO machinery out of the installed system (runs in the target
# chroot, BEFORE initcpio regenerates the initramfs — order set in settings.conf).
dontChroot: false
timeout: 120
script:
  - command: "/opt/veloguard/provision/calamares-target-cleanup.sh"
    timeout: 120
