---
# Stage the kernel into the target /boot BEFORE initcpio (runs on the live
# system, not the chroot — it needs the boot medium at /run/archiso/bootmnt).
dontChroot: true
timeout: 120
script:
  - command: "/opt/veloguard/provision/calamares-stage-kernel.sh ${ROOT}"
    timeout: 120
