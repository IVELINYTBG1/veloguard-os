---
# Install = copy the live system's squashfs to the target. archiso mounts the
# boot medium at /run/archiso/bootmnt; this path matches our profiledef.
unpack:
  - source: /run/archiso/bootmnt/arch/x86_64/airootfs.sfs
    sourcefs: squashfs
    destination: ""
